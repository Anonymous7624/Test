# Root reference only — copy per-app examples:
# - backend/.env.example  → backend/.env
# - frontend/.env.example → frontend/.env.local
# - worker/.env.example   (optional; prefer backend/.env)
#
# Geoapify: set GEOAPIFY_API_KEY in backend/.env and NEXT_PUBLIC_GEOAPIFY_API_KEY in frontend/.env.local
