# Copy to `frontend/.env.local`
# Backend origin only (no /api path). Must match how you reach the API from the browser (use LAN IP when testing from another device).
NEXT_PUBLIC_API_BASE_URL=http://192.168.1.181:8000
# Legacy (optional): NEXT_PUBLIC_API_URL=http://localhost:8000/api
